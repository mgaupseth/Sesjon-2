---
title: "R Notebook"
output: html_notebook
---

This is an [R Markdown](http://rmarkdown.rstudio.com) Notebook. When you execute code within the notebook, the results appear beneath the code. 

Try executing this chunk by clicking the *Run* button within the chunk or by placing your cursor inside it and pressing *Ctrl+Shift+Enter*. 
```{r}
library(tidyverse)
library(lubridate)
library(dplyr)
library(purrr)

#myfunction <- function(arg1, arg2, ... ){
#statements
#return(object)
}


# JP:
# Hei Mina, 
# Dette synes jeg bare ble bedre og bedre utover! Det ser ut til at du fikk taket
# på dette etterhvert :-) 
# 
# Jeg har skrevet ned noen tips til deg, håper de blir nyttige fremover!



```

```{r oppgave 2}
#2.1 Summer to tall

summer_totall <- function(x = 1, y = 4) {
  x <- c(1, 2, 3, 10)
  y <- c(4, 5, 6, 23)
  x + y
}
summer_totall(x + y)

# JP:
# Denne funksjonen vil _alltid_ gi svaret:
# 5 7 9 33
# Det er fordi x og y defineres inne i funksjonen. Da spiller det ingen rolle
# hva vi sender inn. Dersom du heller skriver: 
#  x <- c(1, 2, 3, 10)
#  y <- c(4, 5, 6, 23)
# summer_totall <- function(x = 1, y = 4) {
#  x + y
#}
# Så vil man få en funksjon som har x = 1 og y = 4 som default, men som vil kunne
# gi ulike svar avhengig av hva man sender inn i funksjonen. 
# 
# Tips 1: Navngivning: Her ville jeg bruk "_" mellom "to" og "tall" siden du 
# allerede bruker det mellom "summer" og "to". Bruk det alle steder det er naturlig
# med mellomrom. 
# 
# Tips 2: Default: Det kan være en god idé å tenke over hvilke default man setter
# for funksjonen sin. Dersom man gjør noe feil, f.eks. glemmer y, så kan det være lurt
# å få "NA" eller "0" som default. Det er ingen logisk grunn til at x = 1 og y = 4
# skal være default.
# 
# Ellers en fin funksjon. 


#2.2 Multipliser to tall
multipliser_totall <- function(x = 1, y = 4) {
  x <- c(1, 2, 3, 10)
  y <- c(4, 5, 6, 23)
  x * y
}
multipliser_totall(x * y)

# JP:
# Samme kommentarer som til funksjonen over. 

#2.3 Feilmelding
kvadrer_x <- function(x) {
  x^2
}
kvadrer_x(x^2)
#Obect x not found. Ikke et unikt navn på funksjonen. Gi unikt navn. X har ingen verdi

# JP:
# Dette var ikke feilmeldingen vi var ute etter. Her får man feilmelding dersom 
# man bruker "kvadrer_x()", altså uten input. Det er fordi x mangler default. 
# Ved å legge inn NA eller 0 som default får man ut et ok-fornuftig svar istedenfor
# feilmelding.
# 
# Feilmeldingen du har fått kommer av at du skriver "x^2" som input. Da leter
# R i environment etter et objekt (en vektor, dataframe eller lignende) som heter
# "x". Det finner den ikke, fordi du ikke har laget det. 
# 
# Jeg er usikker på hva du har tenkt, men hvis man skriver "x = 2" istedenfor så
# funker funksjonen. Dersom man definerer x først (x <- 4, f.eks.), og så bruker
# "x^2", så vil funksjonen fungere også (da sendes x^2, som er 4^2 = 16, inn som input, 
# og man får 16^2 = 256 ut).


gjor_noe_kult <- function(x= 2) {
   x <- c(1, 2, 3, 10)
  x^2
}
gjor_noe_kult(x^2)

# JP:
# Coolio!

#2.4 Default value
cor()

# JP:
# Se løsningsforslag.

#2.5 Finnes 2 i vektoren?

finn_to_vektor <- function(x=1) {
  ifelse(test = x == 2, yes = TRUE, no = FALSE)
}

x <- c(1, 2, 3, 10)
y <- c(4, 5, 6, 23) 

finn_to_vektor(x) 

# JP:
# Denne funksjonen funker :) Den gir oss T/F for hvert element i x. 
# 
# Tips: Man trenger ikke "ifelse" når man bare skal returnere TRUE / FALSE. Det
# er fordi den logiske testen i ifelse gir TRUE eller FALSE, og da kan man bare 
# returnere det. Slik:
# finn_to_vektor <- function(x=1) {
#  x == 2
#}
# 
# Tips 2: Her var vi ute etter om tallet 2 fantes i en vektor. Du sjekker hvert
# element i vektoren. Det er nyttig på sin måte, men for å returnere om 2 finnes
# i det hele tatt kan man bruke "any()" rundt logikken din. Da vil den sjekke om
# minst ett av tallene i vektoren er lik 2. Slik:
# 
# finn_to_vektor <- function(x=1) {
#  any(x == 2)
#}
#
# Se også løsningsforslaget for en alternativ løsning.

#2.6 Dårlig idè

# JP:
# Se løsningsforslag. 

#2.7 Partall
er_partall <- function(x) {
if(x %% 2 == 0){return(TRUE)}
  FALSE
}

er_partall(2)

# JP:
# Bra! Denne funksjonen gjør akkurat det den skal! :-)
# 
# Tips: Samme som jeg nevnte over: dersom man ønsker at en funksjon skal returnere
# TRUE eller FALSE, og man har en bit logikk som alt gir TRUE eller FALSE (her:
# x %% 2 == 0), så trenger man ikke å skrive "if()" rundt og så returnere akkurat 
# det samme.


#2.8 IF ELSE IFELSE

# JP:
# Se løsningsforslag, spør meg hvis det er uklart :-) 

#2.9 Funksjonen virker ikke
lag_histogram <-  function(.data, variabel) {
  
  .data %>% 
    ggplot(aes(x = {{variabel}})) + 
    geom_histogram()
  
}
lag_histogram(mtcars, cyl)
#Variabel må være {{for å refere inn i datasett}}

# JP:
# Kanon! Dette er viktig når man jobber med tidyverse-pakker som dplyr og ggplot! :)

```

Add a new chunk by clicking the *Insert Chunk* button on the toolbar or by pressing *Ctrl+Alt+I*.

When you save the notebook, an HTML file containing the code and output will be saved alongside it (click the *Preview* button or press *Ctrl+Shift+K* to preview the HTML file).

The preview shows you a rendered HTML copy of the contents of the editor. Consequently, unlike *Knit*, *Preview* does not run any R code chunks. Instead, the output of the chunk when it was last run in the editor is displayed.
```{r Oppgave 3}
library(lubridate)
#Godmorgen
lag_tid <- function(x = lubridate::now()) {
  case_when(x >= 05 &  x <= 09 ~ "God morgen",
            x >= 10 &  x <= 13  ~ "God formiddag",
            x >= 14 &  x <= 18  ~ "God ettermiddag",
            x >= 19 &  x <= 23  ~ "God kveld")
            }
        lag_tid(23)
            
        
# JP:
# Veldig bra! 
# Her kreves at man sender inn timen på døgnet (00-23). Da er et
# godt tips kalle "x" for "hour" eller noe sånt, slikt at det er lett å skjønne 
# hva som skal inn. 

    
```

```{r Oppgave 4 }
# Konverteringer
fahrenheit_til_celsius <- function(temp_F) {
  Tempc <- (temp_F - 32)* 5/9
  return(Tempc)
}
fahrenheit_til_celsius(32)

celsius_til_fahrenheit <- function(tempc) {
 Tempf <- (tempc*1.8) + 32
  return(Tempf)
}

celsius_til_fahrenheit(100)

# JP:
# Veldig bra!

```

```{r Oppgave 5}
#DingDong
```

```{r Oppgave 6}
# Print_all
```

